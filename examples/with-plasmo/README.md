# [**`trpc-extension`**](../../README.md) (with-plasmo)

### Getting started

Make sure your current working directory is at `/trpc-extension` root.

```bash
npm install
npm run build
npm run dev -w with-plasmo
```
